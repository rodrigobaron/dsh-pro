{
  "name": "dsh-pro",
  "private": true,
  "description": "A personal DeepSeek Harness environment: standalone plugins for artifacts, git review, routines, rewind, search, deep research, and more.",
  "workspaces": [
    "plugins/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "install-plugins": "bash install.sh"
  },
  "devDependencies": {
    "saxes": "^6.0.0",
    "tar": "^7.5.22",
    "undici": "^8.10.0"
  }
}
